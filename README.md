1. This is the supporting front end for the AppAwesome-backend-vanilla-injectedJS. It has 2 tags. One where the name parameters value is supplied as a normal string, "John Doe" & the other where the name parameter's value is supplied as an XSS payload. 
2. Update the android app with the IP of the backend
3. The android app can be simply imported as a project in Android Studio & run
4. The Android Studio version the code has been tested on is 3.6.1
5. The AVD version is API 23, x86

